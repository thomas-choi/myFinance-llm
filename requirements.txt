# Core dependencies
torch>=2.0.0
numpy>=1.24.0
pandas>=2.0.0

# Transformers and LLMs
transformers>=4.40.0
datasets>=2.18.0
accelerate>=0.30.0  # For fine-tuning
peft>=0.10.0  # For parameter-efficient fine-tuning

# Time series forecasting models
# timesfm>=1.3.0
# lag-llama
# gluonts>=0.14.0  # Required for Lag-Llama

# Data and evaluation
scikit-learn>=1.4.0  # For metrics (accuracy_score, f1_score)
yfinance>=0.2.40  # For financial data download